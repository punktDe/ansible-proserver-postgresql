---
postgresql:
  prefix:
    config: /usr/local/etc
  databases: {}
  database_defaults:
    encoding: UTF-8
    lc_collate: en_US.UTF-8
    lc_ctype: en_US.UTF-8
    template: template0
  data_dir: /var/db/postgresql/data9
  init_command: /usr/local/etc/rc.d/postgresql initdb
  user: pgsql
